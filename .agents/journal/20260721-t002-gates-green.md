# 20260721 — T-002: preset setup verified, gates green

## Verify gates on merged main — 2026-07-21T10:05:00Z

- Did: after PR #1 (genesis init) merged to main (8413d42), restarted the
  session branch from origin/main, ran `npm install` (already satisfied from
  the pre-merge verification run) and `./scripts/agentic gates --tier fast`.
- Result: exit 0 — format/lint/typecheck/test/designs/integrity/memory all
  PASS (coverage skipped: optional, no command bound). The genesis integrity
  failure documented in 20260721-instantiate-finops-framework-mcp.md cleared
  as predicted once init landed on main. Completed T-002 with this evidence.
- Next: T-001 and T-004 need owner input (project description, LICENSE
  confirmation); GitHub repo settings (rulesets import, auto-merge, squash
  message) are owner-side. T-003 (first spec) follows T-001.

## T-001: real project description — 2026-07-21T10:30:00Z

- Did: replaced the "What this project is" placeholder in AGENTS.md and the
  MEMORY.md Project/Current-focus sections with the owner-supplied description:
  an MCP server acting as an agentic interface to the FinOps Framework
  (https://finops.org/framework). Owner explicitly directed working T-001 and
  T-004 in this session (deviation from one-task-per-session noted).
- Result: memory lint + full fast-tier gates pass (output in task evidence).

## T-004: root LICENSE confirmed — 2026-07-21T10:35:00Z

- Did: owner confirmed the root LICENSE is correct as-is: MIT,
  "Copyright (c) 2026 Aaron Soto" — the template author and this project's
  owner are the same person, so no rewrite was needed.
- Result: no file changes; task closed on owner confirmation + verified
  LICENSE content. Gates re-run green via tasks complete.

## Flip branching mode to integration — 2026-07-21T11:05:00Z

- Did: owner directed integration branching ("PRs should be squash merging to
  dev, rolling PR dev -> main"); init had defaulted to trunk. Edited
  approvals.yaml branching.mode -> integration (owner-authorized policy
  change), ran approvals compile: settings/copilot.sh drop the gh-pr-merge
  restriction (task->dev merges are agent-allowed in this mode), main ruleset
  pins merge method to merge-commit, integration-branch.json regenerated.
- Result: approvals check no drift; gates green. Owner must import BOTH
  rulesets (replace the already-imported main-branch, add integration-branch)
  and create/verify the dev branch; release-pr.yml then opens the rolling PR
  on the next dev push (needs Actions "create and approve PRs" enabled).
