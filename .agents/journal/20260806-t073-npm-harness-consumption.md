# 2026-08-06 — T-073: consume the npm harness via .agentic manifest + npm-first shim

**Task:** T-073 (spec `.agents/specs/harness-npm-consumption.md`).

**Did:**

- `.agentic/package.json` (private, sole devDependency
  `@aaronmsoto/agentic-harness ^0.1.0`) + `.agentic/package-lock.json`
  (generated by a real install against npm.pkg.github.com) +
  `.agentic/.npmrc` (scope registry line + `${NPM_TOKEN}` reference —
  no literal credential; lockfile grepped clean for `ghp_`/authToken).
- `scripts/agentic` now probes `.agentic/node_modules` → root
  `node_modules` → vendored `.agentic/harness/dist`, with an actionable
  error naming `npm ci --prefix .agentic` when none exists.
- `agentic.config.json` designs/integrity/memory gate commands changed to
  `./scripts/agentic <cmd>` (protected gates-block edit AUTHORIZED by the
  spec/task; performed via the documented
  `.agents/.cache/policy-edit-ok` override, marker removed immediately).

**Observed (not asserted):**

- Real install from GitHub Packages succeeded with the owner-supplied
  read token (`NPM_TOKEN` env, never echoed/committed).
- Resolution priority PROVEN, not inferred: with both copies present,
  `./scripts/agentic --help` contains the `upgrade` help text (grep 1) —
  a command the vendored CLI lacks (grep 0 on direct invocation) — so
  the npm copy wins; with `.agentic/node_modules` stashed, the vendored
  copy serves the same banner; with both hidden, exit 1 and the install
  hint.
- Root `package.json`/`package-lock.json` byte-unchanged (`git status`
  empty for both) and root `npm ci` succeeded with NO registry auth in
  the environment — the product/registry firewall holds.
- `./scripts/agentic gates --tier all` via the npm harness: PASS
  (format/lint/typecheck/test/designs/integrity/memory/build, e2e
  optional-skip). No version-skew warning — correct, no
  approvals.lock.json exists until T-075's upgrade.

**Next:** T-074 (CI swap; owner has granted package Actions access).
