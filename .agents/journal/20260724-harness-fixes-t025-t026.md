# 2026-07-24 — Review-driven harness fixes T-025/T-026 (pre-port batch)

Owner overnight directive: finish outstanding harness fixes here, then port
everything to the template, which will package the harness as
@aaronsoto/agentic-harness for npm distribution (owner decision). Landed:

- T-025: build/verify token split in records + journal, plus a warning when
  loop.max_total_tokens is set but a runner reports no usage (the cap would
  otherwise silently not see that spend). Review B3-economics-3/-4.
- T-026: loop records its starting branch and terminates actionably if HEAD
  moves off it (rogue-runner test proves terminal:error heartbeat);
  saveTasks writes temp+rename so kills cannot tear tasks.json. Review
  B2-soundness-4.

248 harness tests green; tracker rows added; distribution decision recorded
in .agents/specs/loop-harness-improvements.md. Process note: the integrity
gate correctly blocked this session for appending to its own earlier journal
file after that file merged to main — once merged, journal files are frozen
history even for their author; this new file is the compliant record.
