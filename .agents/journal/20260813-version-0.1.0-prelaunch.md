## Drop version from 0.9.0 to 0.1.0 pre-launch — 2026-08-13T00:00:00Z

- did: Owner call: a first supported-beta launch conventionally starts at
  0.1.0, not 0.9.0 (which reads as "nearly stable/1.0-imminent" — the
  opposite signal from a still-settling initial release). Not a tracked
  `.agents/tasks.json` item — a mechanical, repo-wide literal update with
  clear rationale, done directly. Updated every occurrence of `0.9.0`:
  `SERVER_VERSION` literals in `src/servers/framework/server.ts` and
  `src/servers/focus/server.ts` (hand-maintained, since those modules sit
  in the Cloudflare Worker import graph and can't read `package.json` at
  runtime); `package.json` and `packages/finops-focus-mcp/package.json`;
  `server.json` and `packages/finops-focus-mcp/server.json` (each has two
  version fields — the manifest's own and the nested npm-package entry);
  `.github/ISSUE_TEMPLATE/bug_report.yml`'s version placeholder. Left
  historical records untouched: `.agents/tasks.json` (hash-chained,
  describes what was true when T-010/T-077 ran) and prior journal entries
  — those record history, not current state. Ran
  `npm install --package-lock-only`, which also corrected two pre-existing,
  unrelated drifts in `package-lock.json` (root `version` was stale at
  `1.0.0`, `engines.node` was stale at `>=20` vs `package.json`'s `>=22`) —
  confirmed via `git diff --stat` that no dependency entries changed, only
  those metadata fields. Recorded the amendment in `decisions.md`
  (2026-08-13, amends T-077's 2026-08-07 version call) and updated
  `MEMORY.md`/`activeContext.md`'s current-state text and forward-looking
  "Next steps" (the manual-npm-publish step now says 0.1.0).
- result: `./scripts/agentic gates` full run PASS (format, lint, typecheck,
  test — 414 tests, designs, integrity, memory). `node
  scripts/gen-mcp-surface.mjs` produced no diff (version isn't part of the
  captured surface). Live stdio probes against rebuilt `dist/` confirm both
  servers' `initialize` response reports `serverInfo.version: "0.1.0"`
  (framework: `finops-framework-mcp`; focus: `finops-focus-mcp`) —
  `tests/version-sync.test.ts` enforces this stays true going forward.
- implementer notes: no code files under `src/` beyond the two
  `SERVER_VERSION` literals needed touching — everything else was
  metadata/config. `packages/finops-focus-mcp/package.json` and
  `server.json` are hand-maintained source (tracked in git; only `dist/`
  and `data/focus` under that directory are pack-time generated), so
  editing them directly is correct, not a generated-output violation.
- next: no other tracked tasks pending. Remaining work is the owner-gated
  pre-publish/deploy checklist in `activeContext.md`'s "Next steps" (now
  referencing 0.1.0 throughout).
