## Owner correction: strip auto-appended PR-body attribution — 2026-07-15

- did: owner flagged a "Generated by Claude Code" footer on merged PR #2.
  Root cause: PR-creation tooling auto-appends the footer AFTER the
  submitted body (verified: git history clean; update path does NOT
  re-append). Scrubbed PR #1 and #2 bodies. Hardened: gates-fast now fails
  any PR whose body carries an AI-attribution footer (body passed via env,
  injection-safe); ship skill gains a post-create scrub step; patterns.md
  records the failure mode.
- result: both PR bodies verified clean; ci.yml yaml-valid; memory lint ok.
- next: land via the integration flow (task branch → dev PR → squash).
