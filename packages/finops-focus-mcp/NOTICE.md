# NOTICE

## FOCUS specification text — FinOps Open Cost and Usage Specification project (CC BY 4.0)

This package contains and redistributes specification text from the
**FOCUS** (FinOps Open Cost & Usage Specification) project, retrieved from
<https://github.com/FinOps-Open-Cost-and-Usage-Spec/FOCUS_Spec> at git tags
`v1.0` and `v1.2` (`data/focus/1.0/`, `data/focus/1.2/`: column
definitions, attribute definitions, glossary, changelog).

That content is licensed under the **Creative Commons Attribution 4.0
International license (CC BY 4.0)**:
<https://creativecommons.org/licenses/by/4.0/>.

Required attribution: this specification text is the work of the **FOCUS
project contributors** (FinOps Foundation). Every column and attribute
record embeds its `introduced_version` and a canonical `ColumnId`/slug
tying it back to the upstream file it was derived from.

### Modifications

In accordance with CC BY 4.0 §3(a)(1)(B), note that this package **adapts**
the original material: the source `.mdpp`-included markdown files are
parsed into structured JSON (`requirements[]`, typed content-constraint
fields, `introduced_version`) alongside the original markdown served
verbatim; a cross-version diff (`data/focus/derived/diff-1.0-1.2.json`) and
a KPI-to-FOCUS-column mapping (`data/focus/derived/kpi-mapping.json`) are
original derived works, not part of the upstream spec. All derived content
is marked `official: false` in its records and carries an UNOFFICIAL banner
in tool output. **The FOCUS project and the FinOps Foundation do not
endorse this package or its use of the material.**

## FOCUS official sample data — FOCUS project contributors (CC BY 4.0)

`data/focus/samples/1.0/official/` bundles the official 1,000-row sample
dataset published by the FOCUS-Sample-Data project
(<https://github.com/FinOps-Open-Cost-and-Usage-Spec/FOCUS-Sample-Data>,
`FOCUS-1.0/focus_sample.csv`), retrieved unmodified and licensed under
**CC BY 4.0**. It is bundled solely so `calculate_kpi` has ground-truth
sample data to compute against — the FOCUS project does not endorse this
package or its use of the data.

`data/focus/samples/*/synthetic/` are **synthetic** fixtures generated by
this project's seeded generator — not real billing data, not published by
the FOCUS project.

## This package's code

The code in this package (the MCP server implementation) is licensed under
the MIT license — see `LICENSE`. FOCUS™ and the FOCUS logo are trademarks
of the FinOps Foundation; this package is an independent, unofficial
integration and is not affiliated with or endorsed by the FinOps Foundation
or the FOCUS project.

## Source

Built from the `focus` server in
<https://github.com/aaronmsoto/finops-framework-mcp> — see that
repository for source, tests, and full provenance details (including
per-sample `PROVENANCE.json` records).
